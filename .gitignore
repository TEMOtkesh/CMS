# Dependencies / vendor
vendor/

# Environment / config with secrets
# config/db.php — included in repo (default XAMPP values, no secrets)

# Uploaded files (track structure, not content)
uploads/*
!uploads/.gitkeep

# Logs
logs/*
!logs/.gitkeep

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
~$*
